IMPORTANT NOTES

Please import the following in the terminal
in some case it may include a "py -m" before it

pip install keyboard

pip install pyautogui

pip install time (Maybe not sure but do it anyways)
